mixin video(item)
    div
        h4
            a(href=`/videos/${item.id}`)=item.title
        p=item.description
        ul
            each hashtag in item.hashtags
                li=hashtag
        small=item.createdAt